import Root from './panel.svelte';
import Header from './panelHeader.svelte';
import Footer from './panelFooter.svelte';

export {
    Root as Panel,
    Header as PanelHeader,
    Footer as PanelFooter
}